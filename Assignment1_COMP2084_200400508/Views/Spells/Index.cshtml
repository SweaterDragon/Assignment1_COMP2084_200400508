@model IEnumerable<Assignment1_COMP2084_200400508.Models.Spell>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>
@if (User.Identity.IsAuthenticated)
{
<p>
    <a asp-action="Create">Create New</a>
</p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TomeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Art)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Difficulty)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TomeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Art)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Difficulty)
            </td>
@if(User.Identity.IsAuthenticated) { 
            <td>
                <a asp-action="Details" asp-route-id="@item.SpellId">Details</a> |
                <a asp-action="Edit" asp-route-id="@item.SpellId">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.SpellId">Delete</a>
            </td>
}
        </tr>
}
    </tbody>
</table>
