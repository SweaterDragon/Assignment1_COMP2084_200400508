@model IEnumerable<Assignment1_COMP2084_200400508.Models.Tome>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>
@if (User.Identity.IsAuthenticated)
{
<p>
    <a asp-action="Create">Create New</a>
</p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pages)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsRented)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pages)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsRented)
            </td>
@if(User.Identity.IsAuthenticated) { 
            <td>
                <a asp-action="Edit" asp-route-id="@item.TomeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TomeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TomeId">Delete</a>
            </td>
}
        </tr>
}
    </tbody>
</table>
